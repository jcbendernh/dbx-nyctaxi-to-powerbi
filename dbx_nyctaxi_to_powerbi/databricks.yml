# This is a Databricks asset bundle definition for dbx_nyctaxi_to_powerbi.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_nyctaxi_to_powerbi
  uuid: cbc7a85f-dfd6-41de-8f17-dd307b11e995

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3637652851714791.11.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3637652851714791.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/jackbender@mngenvmcap738982.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jackbender@mngenvmcap738982.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: jackbender@mngenvmcap738982.onmicrosoft.com
        level: CAN_MANAGE
